{
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run Test",
            "program": "${workspaceRoot}/node_modules/protractor/bin/protractor",
            "args": [
                "${workspaceRoot}/conf/conf.js"
            ],
            "cwd": "${workspaceRoot}"
        }
    ]
}